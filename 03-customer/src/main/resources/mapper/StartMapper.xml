<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itStudy.dao.StartDao">

    <select id="homeStart" resultType="com.itStudy.entity.Article">
        SELECT id, ref1, creator, title, timeUpdate, text, numReply, numLike, audit,
            draft,  type, cat1, numStart
        FROM `start` s
        LEFT JOIN article a ON a.`id` = s.`aId`
        WHERE
        s.`userId` = #{userId}
        ORDER BY s.`createTime` DESC
        LIMIT #{startIndex}, 10
    </select>




</mapper>