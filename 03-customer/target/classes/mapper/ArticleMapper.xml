<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itStudy.dao.ArticleDao">


    <select id="homeArticle" parameterType="Integer" resultType="com.itStudy.entity.Article">
        SELECT id, ref1, creator, title, timeUpdate, text, numReply, numLike, numStart, numStart, type
        FROM article
        WHERE
        cat1 = #{cat1} AND
        ref2 = 0 AND
        draft = 0 AND
        audit = 0 AND
        form = 0 AND
        delFlag = 0
        ORDER BY numStart DESC, id DESC
        LIMIT #{startIndex}, 10
    </select>

    <select id="historyArticle" parameterType="Integer" resultType="com.itStudy.entity.Article">
        SELECT id, ref1, creator, title, timeUpdate, text, numReply, numLike, type
        FROM article
        WHERE
        id in
        <foreach collection="list" item="articleIdList" separator="," open="(" close=") AND " index="">
            #{articleIdList, jdbcType=NUMERIC}
        </foreach>
        draft = 0 AND
        audit = 0 AND
        form = 0 AND
        delFlag = 0
        ORDER BY FIELD
        <foreach collection="list" item="articleIdList" separator="," open="(id," close=") " index="">
            #{articleIdList, jdbcType=NUMERIC}
        </foreach>
        LIMIT 10
    </select>

    <select id="myhomeArticle" parameterType="Integer" resultType="com.itStudy.entity.Article">
        SELECT id, ref1, creator, title, timeUpdate, text, numReply, numLike, audit, draft,
        type, cat1, numStart
        FROM article
        WHERE
        ref1 = #{myId} AND
        ref2 = 0 AND
        delFlag = 0
        ORDER BY draft DESC, audit DESC, id DESC
        LIMIT #{startIndex}, 10
    </select>

    <update id="articleUpdateStartAdd">
        UPDATE article SET numStart = numStart + 1
        WHERE id = #{articleId}
    </update>

    <update id="articleUpdateStartDec">
        UPDATE article SET numStart = numStart - 1
        WHERE id = #{articleId}
    </update>



</mapper>